{
  "name": "loregraph",
  "version": "0.1.0",
  "description": "Deterministic, layered code knowledge graph for any JS/TS repo, with an MCP server for agents.",
  "type": "module",
  "bin": { "loregraph": "bin/loregraph.mjs" },
  "engines": { "node": ">=18" },
  "dependencies": {
    "ignore": "^5.3.2",
    "typescript": "^5.5.4"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  },
  "scripts": {
    "test": "vitest run"
  }
}
